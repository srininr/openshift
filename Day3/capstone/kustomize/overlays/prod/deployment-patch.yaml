apiVersion: apps/v1
kind: Deployment
spec:
  selector:
    matchLabels:
      app: node-app-prod
  template:
    metadata:
      labels:
        app: node-app-prod
    spec:
      containers:
        - name: node-app
          env:
            - name: CAPSTONE
              value: "Green"